# dependencies
node_modules/

# next.js
.next/
/out/
next-env.d.ts
*.tsbuildinfo

# env / secrets — never commit
.env
.env.local
.env*.local

# os
.DS_Store

# local scratch (HEIC test photos, conversion output) — never commit
/scratch/

# local Claude Code dev-server / preview config (machine-specific)
.claude/launch.json
